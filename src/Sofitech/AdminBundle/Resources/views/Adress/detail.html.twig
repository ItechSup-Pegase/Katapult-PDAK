
        <tr>
            <td colspan="2" bgcolor="orange"> Adresse de {{ entity.type == 'contact' ? 'contact' : 'facturation' }}</td>
        </tr>
        <tr>
            <th>Adresse</th>
            <td>{{ entity.street }}</td>
        </tr>
        <tr>
            <th>Ville</th>
            <td>{{ entity.city }}</td>
        </tr>
        <tr>
            <th>Code postal</th>
            <td>{{ entity.zipcode }}</td>
        </tr>
        <tr>
            <th>Pays</th>
            <td>{{ entity.country }}</td>
 		</tr>
